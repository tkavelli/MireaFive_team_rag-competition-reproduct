# Core dependencies
pandas==2.3.3
numpy==2.2.6
tqdm==4.67.1
requests==2.32.5

# ML and embeddings
# NOTE: Install PyTorch 2.8.0 separately with CUDA 12.8 support:
# pip3 install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
transformers==4.57.1
sentence-transformers==5.1.2

# Vector search
faiss-cpu==1.12.0
# Note: For GPU acceleration, install faiss-gpu manually:
# pip install faiss-gpu==1.12.0

# Text processing
scikit-learn==1.7.2
tiktoken==0.12.0
PyYAML==6.0.3

# Development tools (optional, for code quality)
pytest>=7.0.0
black>=22.0.0
ruff>=0.0.200

# Note: For evaluation metrics (matplotlib, seaborn) install separately if needed
# They are not required for running the main pipeline
